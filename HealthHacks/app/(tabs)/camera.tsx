import React from 'react';
import { View, Text } from 'react-native';
import CameraScreen from '../../screens/CameraScreen'; // ✅ relative path

export default function CameraTab() {
  return <CameraScreen />;
}